import {CustomMaterialTemplate} from '../types/custom-materials';const KEY='renovio-custom-materials';export const getCustomMaterials=():CustomMaterialTemplate[]=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]') as CustomMaterialTemplate[]}catch{return[]}};export const saveCustomMaterial=(item:CustomMaterialTemplate)=>{const all=getCustomMaterials(),index=all.findIndex(x=>x.id===item.id);if(index<0)all.unshift(item);else all[index]=item;localStorage.setItem(KEY,JSON.stringify(all))};export const deleteCustomMaterial=(id:string)=>localStorage.setItem(KEY,JSON.stringify(getCustomMaterials().filter(x=>x.id!==id)));
