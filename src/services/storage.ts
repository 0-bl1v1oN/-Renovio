import {RenovationProject} from '../types/project'; const KEY='renovio-projects'; export const getProjects=():RenovationProject[]=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}; export const saveProject=(p:RenovationProject)=>{const all=getProjects(),i=all.findIndex(x=>x.id===p.id);if(i>=0)all[i]=p;else all.unshift(p);localStorage.setItem(KEY,JSON.stringify(all))}; export const deleteProject=(id:string)=>localStorage.setItem(KEY,JSON.stringify(getProjects().filter(x=>x.id!==id)));
