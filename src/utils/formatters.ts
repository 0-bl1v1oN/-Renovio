export const parseNumber=(v:string|number)=>typeof v==='number'?v:Number(v.replace(',','.').trim()); export const round=(n:number,d=2)=>Math.round((n+Number.EPSILON)*10**d)/10**d; export const rub=(n:number)=>new Intl.NumberFormat('ru-RU',{style:'currency',currency:'RUB',maximumFractionDigits:0}).format(n); export const area=(n:number)=>`${n.toLocaleString('ru-RU',{minimumFractionDigits:2,maximumFractionDigits:2})} м²`; export const length=(n:number)=>`${round(n)} м`;
